import { SkydiveScene } from "./SkydiveScene"

export const Skydive = () => {
    return (
        <section className="skydive h-screen">
            <h2 className="sr-only">Placeholder component for skydive</h2>

            <SkydiveScene />
        </section>
    )
}